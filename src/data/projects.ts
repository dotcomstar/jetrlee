export interface Project {
  name: string;
  description: string;
  shortDescription: string;
  image: string;
  slug: string;
  url: string;
  startDate: string;
  endDate: string;
  isCurrent: string;
}

export default [];
